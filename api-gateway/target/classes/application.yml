server:
  port: 4004

spring:
  cloud:
    gateway:
      routes:
        - id: patient-service
          uri: http://patient-service:4000
          predicates:
            - Path=/api/**
          filters:
            - RewritePath=/api/(.*), /$1